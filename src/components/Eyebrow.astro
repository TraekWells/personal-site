<p class="eyebrow"><slot /></p>

<style>
  .eyebrow {
    display: flex;
    align-items: center;
    gap: var(--space-3xs);
    color: hsl(var(--color-primary-darker));
    font-weight: var(--font-weight-bolder);
    font-size: var(--font-size-sm);
    text-transform: uppercase;
    letter-spacing: var(--letter-spacing-tight);
  }
</style>
