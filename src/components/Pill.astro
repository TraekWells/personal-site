<span class="pill"><slot /></span>

<style>
  .pill {
    background-color: hsl(var(--color-base-white));
    padding: var(--space-3xs) var(--space-2xs);
    border-radius: var(--border-radius-lg);
    border: 1px solid hsl(var(--color-base-light));
  }
</style>
