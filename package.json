{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/content": "^2.2.2",
    "nuxt": "3.0.0",
    "nuxt-icon": "^0.1.8",
    "sitemap": "^7.1.1"
  },
  "dependencies": {
    "sass": "^1.56.2"
  }
}
